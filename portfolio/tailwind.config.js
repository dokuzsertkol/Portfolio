const config = {
    darkMode: "class",
    content: [
        "./app/**/*.{js,ts,jsx,tsx,mdx}",
    ],
    theme: {
        extend: {
            colors: {
                l_base: "#BFECFF",
                d_base: "#070D12",
            },
        },
    },
    plugins: [],
};

export default config;
